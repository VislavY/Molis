<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.vyapps.molis" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Molis" >
        <activity
            android:name=".screens.projects.ProjectsActivity"
            android:label="@string/projects_screen_label">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".screens.projectPage.ProjectPageActivity"
            android:label="@string/task_list_screen_label"
            android:parentActivityName=".screens.projects.ProjectsActivity"
            android:theme="@style/Theme.Molis.NoActionBar">
        </activity>
    </application>

</manifest>